pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'Hello build'
            }
        }  
    stage('testing') {
            steps {
                echo 'Hello testing'
            }
        }   
     stage('deploy') {
            steps {
                echo 'Hello deploy'
            }
        }
    }
}
